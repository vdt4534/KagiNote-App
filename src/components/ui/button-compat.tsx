// Compatibility re-export for gradual migration
// This allows us to update imports without breaking existing code

export { Button, buttonVariants } from './button-new'
export type { ButtonProps } from './button-new'